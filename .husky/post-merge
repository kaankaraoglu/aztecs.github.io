#!/usr/bin/env sh
# Discard local changes to auto-fetched data files after pulls/merges.
# These files are maintained by the fetch-data.yml CI workflow — local
# modifications are never intentional and block future checkouts/pulls.
git checkout -- src/data/rio-mythicplus.json src/data/wcl-progression.json src/data/wcl-stats.json 2>/dev/null
